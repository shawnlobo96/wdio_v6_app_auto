# This script is build with Webdriver IO v6

- Clone the repo

- `export BROWSERSTACK_USERNAME=username`
- `export BROWSERSTACK_ACCESS_KEY=access_key`
- `export APP=bs://<app-id>`

Run `npm i`

Run `npm run android.bs`
